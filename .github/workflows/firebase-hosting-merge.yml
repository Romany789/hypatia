# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - master

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2 # Use the latest version of actions/checkout

      - name: Setup Node.js
        uses: actions/setup-node@v2 # Use the latest version of actions/setup-node
        with:
          node-version: 14 # Choose the Node.js version you need

      - name: Install dependencies
        run: npm install

      - name: Build and deploy
        run: |
          # Build your project here (e.g., npm run build)
          # Deploy to Firebase Hosting
          npx firebase deploy --only hosting

        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_ACCESS_TOKEN }} # Make sure to set up this secret in your repository settings
